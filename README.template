# Dolibarr on Docker

Docker image for Dolibarr with auto installer on first boot.

Based on [tuxgasy/docker-dolibarr](https://github.com/tuxgasy/docker-dolibarr):
- built on alpine base
- nginx + php-fpm instead of apache
